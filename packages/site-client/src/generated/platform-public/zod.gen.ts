// This file is auto-generated by @hey-api/openapi-ts

import * as z from 'zod';

export const zAcceptedPublicCommandReceipt = z.strictObject({
    commandId: z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    deliveryState: z.literal('not_applicable'),
    observedAt: z.iso.datetime(),
    requestDigest: z.string().regex(/^[0-9a-f]{64}$/),
    state: z.literal('accepted')
});

export const zAccountEntitlementSummary = z.strictObject({
    capabilityKey: z.string().regex(/^[a-z0-9][a-z0-9._:-]{0,127}$/),
    entitlementGrantRef: z.string().min(1).max(256),
    expiresAt: z.iso.datetime().nullable(),
    safeLabel: z.string().min(1).max(160),
    state: z.enum([
        'active',
        'expired',
        'revoked'
    ])
});

export const zAccountPlanSummary = z.strictObject({
    automaticRenewal: z.literal(false),
    planRef: z.string().min(1).max(256),
    planVersionRef: z.string().min(1).max(256),
    safeLabel: z.string().min(1).max(160)
});

export const zAccountRecoveryTransaction = z.strictObject({
    expiresAt: z.iso.datetime(),
    recoveryState: z.enum([
        'proof_required',
        'review_required',
        'ready'
    ]),
    transactionRef: z.string().min(1).max(128)
});

export const zAcquisitionSourceSummary = z.strictObject({
    acquiredAt: z.iso.datetime(),
    kind: z.enum([
        'redemption',
        'admin_grant',
        'program_window'
    ]),
    sourceRef: z.string().min(1).max(256)
});

export const zAccountProduct = z.strictObject({
    creditGrantRefs: z.array(z.string().min(1).max(256)).max(256),
    effectiveAt: z.iso.datetime(),
    entitlements: z.array(zAccountEntitlementSummary).max(256),
    expiresAt: z.iso.datetime().nullable(),
    kind: z.enum([
        'free',
        'credit_pack',
        'subscription',
        'bundle'
    ]),
    plan: zAccountPlanSummary.nullable(),
    productRef: z.string().min(1).max(256),
    productVersionRef: z.string().min(1).max(256),
    safeLabel: z.string().min(1).max(160),
    source: zAcquisitionSourceSummary,
    state: z.enum([
        'active',
        'expired',
        'revoked'
    ])
});

export const zAuthPending = z.strictObject({
    challengeKind: z.enum(['totp', 'recovery']),
    expiresAt: z.iso.datetime(),
    transactionRef: z.string().min(1).max(128)
});

export const zBeginTotpEnrollmentInput = z.strictObject({
    ceremonyAction: z.literal('begin')
});

export const zBeginTotpRecoveryReplacementInput = z.strictObject({
    ceremonyAction: z.literal('begin'),
    proofCode: z.string().min(8).max(256),
    replacementFactor: z.literal('totp'),
    transactionSecret: z.string().min(32).max(2048)
});

export const zChannelChallengeProofInput = z.strictObject({
    challengeRef: z.string().min(16).max(128),
    proofSecret: z.string().min(16).max(2048)
});

export const zCommandInput = z.strictObject({
    commandRef: z.string().max(128)
});

export const zCommandReceipt = z.strictObject({
    commandId: z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    committedAt: z.iso.datetime(),
    receiptRef: z.string().min(1).max(256),
    requestDigest: z.string().regex(/^[0-9a-f]{64}$/),
    state: z.literal('committed')
});

export const zAccountRecoveryTransactionResponse = z.strictObject({
    receipt: zCommandReceipt,
    transaction: zAccountRecoveryTransaction
});

export const zAuthPendingResponse = z.strictObject({
    pending: zAuthPending,
    receipt: zCommandReceipt
});

export const zCommandReceiptResponse = z.strictObject({
    receipt: zCommandReceipt
});

export const zCommittedCommandReconciliation = z.strictObject({
    kind: z.literal('terminal'),
    outcome: z.literal('committed')
});

export const zCommittedDeliveryConsumedPublicCommandReceipt = z.strictObject({
    commandId: z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    deliveryState: z.literal('first_claim_consumed'),
    observedAt: z.iso.datetime(),
    requestDigest: z.string().regex(/^[0-9a-f]{64}$/),
    state: z.literal('committed')
});

export const zCommittedDeliverySupersededPublicCommandReceipt = z.strictObject({
    commandId: z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    deliveryState: z.literal('superseded'),
    observedAt: z.iso.datetime(),
    requestDigest: z.string().regex(/^[0-9a-f]{64}$/),
    state: z.literal('committed')
});

export const zCommittedPublicCommandReceipt = z.strictObject({
    commandId: z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    deliveryState: z.literal('not_applicable'),
    observedAt: z.iso.datetime(),
    requestDigest: z.string().regex(/^[0-9a-f]{64}$/),
    state: z.literal('committed')
});

export const zCommittedSupersededPublicCommandReceiptResponse = z.strictObject({
    receipt: zCommittedDeliverySupersededPublicCommandReceipt,
    reconciliation: zCommittedCommandReconciliation
});

export const zCommittedTerminalPublicCommandReceiptResponse = z.strictObject({
    receipt: zCommittedPublicCommandReceipt,
    reconciliation: zCommittedCommandReconciliation
});

export const zCompleteSessionMfaInput = z.strictObject({
    code: z.string().min(6).max(32)
});

export const zConfirmTotpRecoveryReplacementInput = z.strictObject({
    ceremonyAction: z.literal('confirm'),
    confirmationCode: z.string().min(6).max(32),
    enrollmentTransactionRef: z.string().min(16).max(128),
    factorReplacementCapability: z.string().min(32).max(2048),
    replacementFactor: z.literal('totp')
});

/**
 * Exact non-negative integer amount; encoded as a decimal string to avoid JS precision loss.
 */
export const zCreditAmount = z.string().regex(/^(?:0|[1-9][0-9]{0,37})$/);

export const zCreditBucketSummary = z.strictObject({
    available: zCreditAmount,
    bucketClass: z.enum([
        'daily',
        'period',
        'permanent'
    ]),
    consumed: zCreditAmount,
    expiredOrReversed: zCreditAmount,
    grantCount: z.int().gte(0),
    held: zCreditAmount,
    issued: zCreditAmount
});

export const zCreditGrantDetail = z.strictObject({
    available: zCreditAmount,
    bucketClass: z.enum([
        'daily',
        'period',
        'permanent'
    ]),
    consumed: zCreditAmount,
    creditProgramRevisionRef: z.string().min(1).max(256),
    effectiveAt: z.iso.datetime(),
    expiresAt: z.iso.datetime().nullable(),
    grantId: z.string().regex(/^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/),
    held: zCreditAmount,
    issued: zCreditAmount,
    source: zAcquisitionSourceSummary,
    state: z.enum([
        'active',
        'exhausted',
        'expired',
        'reversed'
    ]),
    unit: z.string().min(1).max(64)
});

export const zCreditUnitSummary = z.strictObject({
    buckets: z.array(zCreditBucketSummary).max(3),
    unit: z.string().min(1).max(64)
});

export const zEmailChangeCompletionInput = z.strictObject({
    currentAddressChallenge: zChannelChallengeProofInput,
    reauthenticationProof: z.string().min(32).max(2048),
    replacementAddressChallenge: zChannelChallengeProofInput
});

export const zEmailChangeStartInput = z.strictObject({
    reauthenticationProof: z.string().min(32).max(2048),
    replacementEmail: z.email().max(191)
});

/**
 * The current-address and replacement-address challenge references are distinct, independently hashed/consumed, and must both be confirmed before the primary identifier can change.
 */
export const zEmailChangeTransaction = z.strictObject({
    currentAddressChallengeRef: z.string().min(16).max(128),
    expiresAt: z.iso.datetime(),
    replacementAddressChallengeRef: z.string().min(16).max(128),
    transactionRef: z.string().min(1).max(128)
});

export const zEmailChangeTransactionResponse = z.strictObject({
    receipt: zCommandReceipt,
    transaction: zEmailChangeTransaction
});

export const zEmailVerificationTransaction = z.strictObject({
    deliveryState: z.enum([
        'queued',
        'sent',
        'rate_limited'
    ]),
    expiresAt: z.iso.datetime(),
    transactionRef: z.string().min(1).max(128)
});

export const zEmailVerificationTransactionResponse = z.strictObject({
    receipt: zCommandReceipt,
    transaction: zEmailVerificationTransaction
});

export const zErrorCode = z.enum([
    'INVALID_REQUEST',
    'CONTRACT_VERSION_UNSUPPORTED',
    'AUTHENTICATION_REQUIRED',
    'AUTHENTICATION_FAILED',
    'AUTH_TRANSACTION_EXPIRED',
    'MFA_REQUIRED',
    'FORBIDDEN',
    'NOT_FOUND',
    'CONFLICT',
    'RATE_LIMITED',
    'RISK_UNAVAILABLE',
    'SITE_UNAVAILABLE',
    'OUTCOME_UNKNOWN',
    'INTERNAL_UNAVAILABLE',
    'REDEEM_NOT_ACCEPTED',
    'REDEEM_TEMPORARILY_UNAVAILABLE',
    'IDEMPOTENCY_CONFLICT',
    'ACQUISITION_CHANNEL_DISABLED'
]);

export const zErrorResponse = z.strictObject({
    code: zErrorCode,
    correlationId: z.string().max(128),
    receiptRef: z.string().max(256).optional(),
    requestId: z.string().max(128),
    retryClass: z.enum([
        'never',
        'after_delay',
        'after_user_action',
        'same_identity',
        'reconcile_receipt'
    ]),
    safeMessage: z.string().max(512)
});

export const zIdentifierInput = z.strictObject({
    email: z.email().max(191)
});

export const zIdentitySession = z.strictObject({
    createdAt: z.iso.datetime(),
    current: z.boolean(),
    deviceLabel: z.string().max(128),
    expiresAt: z.iso.datetime(),
    lastSeenAt: z.iso.datetime(),
    sessionRef: z.string().min(1).max(128),
    status: z.enum([
        'active',
        'revoked',
        'expired'
    ])
});

export const zIdentitySessionList = z.strictObject({
    revision: z.string().min(1).max(128),
    sessions: z.array(zIdentitySession).max(200)
});

export const zLocalePolicy = z.strictObject({
    allowedLocales: z.array(z.string().min(2).max(35)).min(1).max(64),
    defaultLocale: z.string().min(2).max(35)
});

export const zMfaReauthenticationInput = z.strictObject({
    challengeKind: z.enum(['totp', 'recovery']),
    proofCode: z.string().min(6).max(64),
    stage: z.literal('mfa'),
    transactionRef: z.string().min(16).max(128)
});

/**
 * Returned by an exact idempotent retry after the first one-time delivery was claimed. The previous payload is never replayed; the caller must read the durable receipt and, when offered, start the permitted replacement command with the caller-held receipt recovery capability.
 */
export const zOneTimeDeliveryUnavailable = z.strictObject({
    commandId: z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    kind: z.literal('delivery_unavailable'),
    receiptRef: z.string().min(1).max(256),
    requestDigest: z.string().regex(/^[0-9a-f]{64}$/)
});

/**
 * Available only on the fresh code-set command's first successful response claim.
 */
export const zOneTimeRecoveryCodeSetDelivery = z.strictObject({
    commandId: z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    generatedAt: z.iso.datetime(),
    recoveryCodes: z.array(z.string().min(8).max(64)).min(8).max(16),
    requestDigest: z.string().regex(/^[0-9a-f]{64}$/)
});

export const zOtpInput = z.strictObject({
    code: z.string().min(6).max(32)
});

export const zOutcomeUnknownPublicCommandReceipt = z.strictObject({
    commandId: z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    deliveryState: z.literal('not_applicable'),
    observedAt: z.iso.datetime(),
    requestDigest: z.string().regex(/^[0-9a-f]{64}$/),
    state: z.literal('outcome_unknown')
});

export const zPasswordChangeInput = z.strictObject({
    currentPassword: z.string().max(1024),
    newPassword: z.string().min(15).max(1024)
});

export const zPasswordInput = z.strictObject({
    newPassword: z.string().min(15).max(1024),
    transactionSecret: z.string().max(2048)
});

export const zPasswordLoginInput = z.strictObject({
    email: z.email().max(191),
    password: z.string().max(1024),
    returnIntentRef: z.string().min(1).max(128).regex(/^[a-z][a-z0-9_.-]{0,127}$/).optional()
});

export const zPasswordReauthenticationInput = z.strictObject({
    password: z.string().max(1024),
    stage: z.literal('password')
});

export const zPasswordRecoveryCompletionInput = z.strictObject({
    proofCode: z.string().min(8).max(256),
    replacementFactor: z.literal('password'),
    replacementPassword: z.string().min(15).max(1024),
    transactionSecret: z.string().min(32).max(2048)
});

export const zPasswordResetTransaction = z.strictObject({
    expiresAt: z.iso.datetime(),
    transactionRef: z.string().min(1).max(128)
});

export const zPasswordResetTransactionResponse = z.strictObject({
    receipt: zCommandReceipt,
    transaction: zPasswordResetTransaction
});

export const zPendingCommandReconciliation = z.strictObject({
    kind: z.literal('pending'),
    retryAfterSeconds: z.int().gte(1).lte(300)
});

export const zAcceptedPublicCommandReceiptResponse = z.strictObject({
    receipt: zAcceptedPublicCommandReceipt,
    reconciliation: zPendingCommandReconciliation
});

export const zOutcomeUnknownPublicCommandReceiptResponse = z.strictObject({
    receipt: zOutcomeUnknownPublicCommandReceipt,
    reconciliation: zPendingCommandReconciliation
});

export const zPersonalContextActor = z.strictObject({
    avatarUrl: z.url().max(2048).nullable(),
    displayName: z.string().min(1).max(160),
    state: z.literal('active'),
    subjectGeneration: z.string().regex(/^(?:0|[1-9][0-9]{0,18})$/),
    subjectRef: z.string().min(1).max(256)
});

export const zPersonalProjectSummary = z.strictObject({
    displayName: z.string().min(1).max(160),
    executionSpaceRef: z.string().min(1).max(256),
    membershipRevision: z.string().min(1).max(128),
    projectRef: z.string().min(1).max(256),
    workspaceRef: z.string().min(1).max(256)
});

/**
 * Authenticated subject state for the workload-derived Site; never cache across users.
 */
export const zPersonalContext = z.strictObject({
    actor: zPersonalContextActor,
    contextRevision: z.string().min(1).max(128),
    defaultProjectRef: z.string().min(1).max(256),
    expiresAt: z.iso.datetime(),
    issuedAt: z.iso.datetime(),
    personalContextRef: z.string().min(1).max(256),
    productContextRef: z.string().min(1).max(256),
    projects: z.array(zPersonalProjectSummary).min(1).max(256)
});

/**
 * A canonical positive unsigned 64-bit integer encoded as a decimal string.
 */
export const zPositiveUint64String = (z.string().min(1).max(20).regex(/^[1-9][0-9]{0,19}$/)).refine((value) => value.length < 20 || value <= "18446744073709551615", "must fit a positive uint64");

export const zProjectionRevision = z.string().regex(/^(?:0|[1-9][0-9]{0,18})$/);

export const zProjectionFreshness = z.strictObject({
    asOf: z.iso.datetime(),
    lagSeconds: z.int().gte(0).lte(86400),
    revision: zProjectionRevision,
    state: z.enum([
        'current',
        'stale',
        'rebuilding'
    ])
});

export const zAccountProductsResponse = z.strictObject({
    freshness: zProjectionFreshness,
    products: z.array(zAccountProduct).max(512)
});

export const zCreditGrantResponse = z.strictObject({
    freshness: zProjectionFreshness,
    grant: zCreditGrantDetail
});

export const zCreditSummaryResponse = z.strictObject({
    activeHoldCount: z.int().gte(0),
    freshness: zProjectionFreshness,
    units: z.array(zCreditUnitSummary).max(16)
});

/**
 * Immutable, Site-release-published product option safe for an end-user selector. Provider, route, credential, price internals, fallback order, and the hidden orchestration model are deliberately absent. Admission resolves the revision reference to the complete bundle.
 */
export const zPublishedModelOption = z.strictObject({
    availability: z.enum(['available', 'temporarily_unavailable']),
    badges: z.array(z.string().min(1).max(64)).max(16),
    description: z.string().max(512).optional(),
    inputModalities: z.array(z.string().regex(/^[a-z][a-z0-9._-]{0,63}$/)).min(1).max(16),
    label: z.string().min(1).max(128),
    modelOptionRevisionRef: z.string().min(1).max(256),
    optionKey: z.string().regex(/^[a-z][a-z0-9._-]{1,127}$/),
    outputModalities: z.array(z.string().regex(/^[a-z][a-z0-9._-]{0,63}$/)).min(1).max(16),
    supportedEfforts: z.array(z.string().min(1).max(64)).max(16)
});

export const zReauthenticationPending = z.strictObject({
    challengeKind: z.enum(['totp', 'recovery']),
    expiresAt: z.iso.datetime(),
    transactionRef: z.string().min(16).max(128)
});

export const zReauthenticationPendingResponse = z.strictObject({
    pending: zReauthenticationPending,
    receipt: zCommandReceipt
});

export const zReauthenticationProof = z.strictObject({
    authStrengthPolicyRevision: z.string().min(1).max(128),
    expiresAt: z.iso.datetime(),
    issuedAt: z.iso.datetime(),
    reauthenticationProof: z.string().min(32).max(2048),
    sessionEpoch: z.coerce.bigint().gte(BigInt(0)).max(BigInt('9223372036854775807'), { error: 'Invalid value: Expected int64 to be <= 9223372036854775807' }),
    sessionRef: z.string().min(16).max(128),
    userSecurityEpoch: z.coerce.bigint().gte(BigInt(0)).max(BigInt('9223372036854775807'), { error: 'Invalid value: Expected int64 to be <= 9223372036854775807' })
});

/**
 * Available only on the fresh reauthentication command's first successful claim.
 */
export const zOneTimeReauthenticationProofDelivery = z.strictObject({
    commandId: z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    proof: zReauthenticationProof,
    requestDigest: z.string().regex(/^[0-9a-f]{64}$/)
});

export const zReauthenticationResponse = z.union([
    zReauthenticationPendingResponse,
    zOneTimeReauthenticationProofDelivery,
    zOneTimeDeliveryUnavailable
]);

export const zRecoveryCodeSetResponse = z.union([
    zOneTimeRecoveryCodeSetDelivery,
    zOneTimeDeliveryUnavailable
]);

export const zRedemptionCommandCursor = z.strictObject({
    commandId: z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    receiptRef: z.string().min(1).max(256),
    receivedAt: z.iso.datetime(),
    requestDigest: z.string().regex(/^[0-9a-f]{64}$/),
    updatedAt: z.iso.datetime()
});

export const zRedemptionCommandPendingResponse = z.strictObject({
    command: zRedemptionCommandCursor,
    kind: z.enum([
        'accepted',
        'executing',
        'outcome_unknown'
    ]),
    retryAfter: z.iso.datetime()
});

export const zRedemptionCommandReviewResponse = z.strictObject({
    attemptRef: z.string().min(1).max(256),
    caseRef: z.string().min(1).max(256),
    command: zRedemptionCommandCursor,
    kind: z.literal('review_required'),
    reviewExpiresAt: z.iso.datetime()
});

export const zRedemptionConfirmInput = z.strictObject({
    legalAcceptanceRefs: z.array(z.string().min(1).max(128)).max(16),
    previewCredential: z.string().min(32).max(4096).regex(/^\S+$/)
});

export const zRedemptionCreditPreview = z.strictObject({
    amount: zCreditAmount,
    bucketClass: z.enum([
        'daily',
        'period',
        'permanent'
    ]),
    creditProgramRevisionRef: z.string().min(1).max(256),
    expiresAt: z.iso.datetime().nullable(),
    unit: z.string().min(1).max(64)
});

export const zRedemptionEntitlementPreview = z.strictObject({
    capabilityKey: z.string().regex(/^[a-z0-9][a-z0-9._:-]{0,127}$/),
    entitlementTemplateRevisionRef: z.string().min(1).max(256),
    expiresAt: z.iso.datetime().nullable(),
    safeLabel: z.string().min(1).max(160)
});

export const zRedemptionOutputRef = z.strictObject({
    kind: z.enum([
        'subscription_term',
        'entitlement_grant',
        'credit_grant'
    ]),
    outputLineId: z.string().min(1).max(128),
    resourceRef: z.string().min(1).max(256),
    templateRevisionRef: z.string().min(1).max(256)
});

/**
 * The only public request that carries a raw redeem Code. BFF and Platform must redact the field from logs, traces, metrics, analytics, errors, durable requests, and receipts.
 */
export const zRedemptionPreviewInput = z.strictObject({
    code: z.string().min(16).max(256).regex(/^[A-Za-z0-9-]+$/)
});

export const zRedemptionReceipt = z.strictObject({
    commandId: z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    fulfillmentRef: z.string().min(1).max(256),
    outputSetDigest: z.string().regex(/^[0-9a-f]{64}$/),
    outputs: z.array(zRedemptionOutputRef).max(256),
    planRef: z.string().max(256).nullable(),
    planVersionRef: z.string().max(256).nullable(),
    productRef: z.string().min(1).max(256),
    productVersionRef: z.string().min(1).max(256),
    redeemedAt: z.iso.datetime(),
    redemptionId: z.string().regex(/^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/),
    reversalRefs: z.array(z.string().min(1).max(256)).max(64),
    safeCodeFingerprint: z.string().regex(/^[A-Z0-9-]{4,32}$/),
    state: z.enum([
        'fulfilled',
        'reversed',
        'reconciliation_required'
    ]),
    stateObservedAt: z.iso.datetime()
});

export const zRedemptionCommandSucceededResponse = z.strictObject({
    command: zRedemptionCommandCursor,
    kind: z.literal('succeeded'),
    redemption: zRedemptionReceipt
});

export const zRedemptionReceiptResponse = z.strictObject({
    redemption: zRedemptionReceipt
});

export const zRedemptionRejection = z.strictObject({
    code: z.enum(['REDEEM_NOT_ACCEPTED', 'REDEEM_TEMPORARILY_UNAVAILABLE']),
    retryAfter: z.iso.datetime().nullable(),
    retryClass: z.enum([
        'never',
        'after_delay',
        'after_user_action'
    ])
});

export const zRedemptionCommandRejectedResponse = z.strictObject({
    command: zRedemptionCommandCursor,
    kind: z.literal('rejected'),
    rejection: zRedemptionRejection
});

export const zRedemptionCommandResponse = z.union([
    zRedemptionCommandPendingResponse,
    zRedemptionCommandReviewResponse,
    zRedemptionCommandSucceededResponse,
    zRedemptionCommandRejectedResponse
]);

export const zRedemptionTermPreview = z.strictObject({
    action: z.enum([
        'none',
        'new_subscription',
        'extend_from_max',
        'reject_if_active'
    ]),
    automaticRenewal: z.literal(false),
    endsAt: z.iso.datetime().nullable(),
    startsAt: z.iso.datetime().nullable()
});

export const zRedemptionPreview = z.strictObject({
    credits: z.array(zRedemptionCreditPreview).max(64),
    entitlements: z.array(zRedemptionEntitlementPreview).max(128),
    expiresAt: z.iso.datetime(),
    legalTermRefs: z.array(z.string().min(1).max(128)).max(16),
    planRef: z.string().max(256).nullable(),
    planVersionRef: z.string().max(256).nullable(),
    previewCredential: z.string().min(32).max(4096).regex(/^\S+$/),
    previewDigest: z.string().regex(/^[0-9a-f]{64}$/),
    previewRef: z.string().min(1).max(256),
    productKind: z.enum([
        'free',
        'credit_pack',
        'subscription',
        'bundle'
    ]),
    productRef: z.string().min(1).max(256),
    productVersionRef: z.string().min(1).max(256),
    safePlanLabel: z.string().max(160).nullable(),
    safeProductLabel: z.string().min(1).max(160),
    term: zRedemptionTermPreview
});

export const zRedemptionPreviewResponse = z.strictObject({
    preview: zRedemptionPreview,
    receipt: zCommandReceipt
});

export const zRefreshCredentialInput = z.strictObject({
    opaqueCredential: z.string().min(32).max(2048)
});

export const zRegenerateRecoveryCodesInput = z.strictObject({
    recoveryAction: z.literal('regenerate')
});

export const zRegistrationInput = z.strictObject({
    email: z.email().max(191),
    legalAcceptanceRefs: z.array(z.string().max(128)).max(16),
    password: z.string().min(15).max(1024)
});

export const zRejectedCommandReconciliation = z.strictObject({
    kind: z.literal('terminal'),
    outcome: z.literal('rejected')
});

export const zRejectedPublicCommandReceipt = z.strictObject({
    commandId: z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    deliveryState: z.literal('not_applicable'),
    observedAt: z.iso.datetime(),
    requestDigest: z.string().regex(/^[0-9a-f]{64}$/),
    state: z.literal('rejected')
});

export const zRejectedTerminalPublicCommandReceiptResponse = z.strictObject({
    receipt: zRejectedPublicCommandReceipt,
    reconciliation: zRejectedCommandReconciliation
});

export const zRevokeInput = z.strictObject({
    sessionRef: z.string().max(128).optional(),
    target: z.enum([
        'current',
        'single',
        'others',
        'all'
    ])
});

export const zSessionCredentialPair = z.strictObject({
    refreshCredential: z.string().min(32).max(2048),
    refreshCredentialExpiresAt: z.iso.datetime(),
    sessionCredential: z.string().min(32).max(2048),
    sessionCredentialExpiresAt: z.iso.datetime(),
    sessionRef: z.string().min(1).max(128)
});

/**
 * Available only on the fresh command's first successful response claim.
 */
export const zOneTimeSessionCredentialDelivery = z.strictObject({
    commandId: z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    credentials: zSessionCredentialPair,
    requestDigest: z.string().regex(/^[0-9a-f]{64}$/)
});

export const zCreateSessionResponse = z.union([
    zAuthPendingResponse,
    zOneTimeSessionCredentialDelivery,
    zOneTimeDeliveryUnavailable
]);

export const zSessionCredentialPairResponse = z.union([
    zOneTimeSessionCredentialDelivery,
    zOneTimeDeliveryUnavailable
]);

export const zSessionGrantControlAuthorization = z.strictObject({
    audience: z.literal('session.control'),
    purpose: z.literal('control')
});

export const zSessionGrantProjectResource = z.strictObject({
    kind: z.literal('project')
});

export const zSessionGrantReadAuthorization = z.strictObject({
    audience: z.literal('session.read'),
    purpose: z.literal('read')
});

export const zSessionGrantRunResource = z.strictObject({
    kind: z.literal('run'),
    runRef: z.string().min(1).max(256),
    sessionRef: z.string().min(1).max(256)
});

export const zSessionGrantSessionResource = z.strictObject({
    kind: z.literal('session'),
    sessionRef: z.string().min(1).max(256)
});

export const zSessionGrantResource = z.union([
    zSessionGrantProjectResource,
    zSessionGrantSessionResource,
    zSessionGrantRunResource
]);

export const zSessionAccessGrantBinding = z.strictObject({
    authorizationEpoch: zPositiveUint64String,
    credentialEpoch: zPositiveUint64String,
    deploymentRef: z.string().min(1).max(256),
    expiresAt: z.iso.datetime(),
    identitySessionEpoch: zPositiveUint64String,
    identitySessionRef: z.string().min(1).max(256),
    issuedAt: z.iso.datetime(),
    issuer: z.url().min(1).max(512),
    keyRevision: z.string().regex(/^[A-Za-z0-9_-]{1,128}$/),
    membershipEpoch: zPositiveUint64String,
    notBefore: z.iso.datetime(),
    policyEpoch: zPositiveUint64String,
    productContextRef: z.string().min(1).max(256),
    projectRef: z.string().min(1).max(256),
    region: z.string().min(1).max(128),
    resource: zSessionGrantResource,
    restrictionEpoch: zPositiveUint64String,
    revocationEpoch: zPositiveUint64String,
    runtimeEnvironment: z.enum([
        'development',
        'preview',
        'production'
    ]),
    sessionContractRevision: z.string().min(1).max(128),
    siteProjectBindingRef: z.string().min(1).max(256),
    siteRef: z.string().min(1).max(128),
    siteReleaseRef: z.string().min(1).max(128),
    siteSecurityEpoch: zPositiveUint64String,
    subjectGeneration: zPositiveUint64String,
    subjectRef: z.string().min(1).max(256),
    webArtifactDigest: z.string().regex(/^[0-9a-f]{64}$/)
});

export const zSessionAccessGrantInput = z.strictObject({
    productContextRef: z.string().min(1).max(256),
    projectRef: z.string().min(1).max(256),
    purpose: z.enum([
        'read',
        'write',
        'control',
        'stream'
    ]),
    resource: zSessionGrantResource
});

export const zSessionGrantStreamAuthorization = z.strictObject({
    audience: z.literal('session.stream'),
    purpose: z.literal('stream')
});

export const zSessionGrantWriteAuthorization = z.strictObject({
    audience: z.literal('session.write'),
    purpose: z.literal('write')
});

export const zSessionGrantAuthorization = z.union([
    zSessionGrantReadAuthorization,
    zSessionGrantWriteAuthorization,
    zSessionGrantControlAuthorization,
    zSessionGrantStreamAuthorization
]);

export const zSessionAccessGrant = z.strictObject({
    authorization: zSessionGrantAuthorization,
    binding: zSessionAccessGrantBinding,
    credential: z.string().min(32).max(4096).regex(/^[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+$/),
    grantRef: z.string().min(1).max(256)
});

export const zSessionAccessGrantResponse = z.strictObject({
    grant: zSessionAccessGrant
});

export const zSupersedeReauthenticationProofInput = z.strictObject({
    priorCommandId: z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    stage: z.literal('supersede')
});

export const zReauthenticationInput = z.union([
    zPasswordReauthenticationInput,
    zMfaReauthenticationInput,
    zSupersedeReauthenticationProofInput
]);

export const zSupersedeRecoveryCodeSetInput = z.strictObject({
    priorCommandId: z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    recoveryAction: z.literal('supersede')
});

export const zRecoveryCodeRegenerationInput = z.union([
    zRegenerateRecoveryCodesInput,
    zSupersedeRecoveryCodeSetInput
]);

export const zSupersedeRefreshCredentialDeliveryInput = z.strictObject({
    priorCommandId: z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    recoveryAction: z.literal('supersede_refresh_delivery')
});

export const zRefreshSessionInput = z.union([
    zRefreshCredentialInput,
    zSupersedeRefreshCredentialDeliveryInput
]);

export const zSupersedeSessionCredentialDeliveryInput = z.strictObject({
    priorCommandId: z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    recoveryAction: z.literal('supersede_session_delivery')
});

export const zCreateSessionInput = z.union([
    zPasswordLoginInput,
    zSupersedeSessionCredentialDeliveryInput
]);

export const zSessionMfaCompletionInput = z.union([
    zCompleteSessionMfaInput,
    zSupersedeSessionCredentialDeliveryInput
]);

export const zSupersedeTotpEnrollmentInput = z.strictObject({
    ceremonyAction: z.literal('supersede'),
    priorCommandId: z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    priorTransactionRef: z.string().min(16).max(128)
});

export const zSupersedeTotpRecoveryReplacementInput = z.strictObject({
    ceremonyAction: z.literal('supersede'),
    priorCommandId: z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    priorEnrollmentTransactionRef: z.string().min(16).max(128),
    replacementFactor: z.literal('totp')
});

export const zRecoveryInput = z.union([
    zPasswordRecoveryCompletionInput,
    zBeginTotpRecoveryReplacementInput,
    zConfirmTotpRecoveryReplacementInput,
    zSupersedeTotpRecoveryReplacementInput
]);

/**
 * Safe instruction naming the one new command allowed to consume the caller-held receipt recovery capability. The response returns no bearer capability. Its binding digest covers the Site release, workload, operation, original command, transaction, and expiry; consumption and orphan invalidation are atomic.
 */
export const zSupersedingCeremony = z.strictObject({
    bindingDigest: z.string().regex(/^[0-9a-f]{64}$/),
    expiresAt: z.iso.datetime(),
    invalidatesPriorDelivery: z.literal(true),
    operationId: z.enum([
        'createIdentitySession',
        'completeSessionMfa',
        'refreshIdentitySession',
        'beginTotpEnrollment',
        'regenerateRecoveryCodes',
        'reauthenticateIdentitySession',
        'completeAccountRecovery'
    ]),
    transactionRef: z.string().min(16).max(128)
});

export const zSupersedingCeremonyReconciliation = z.strictObject({
    ceremony: zSupersedingCeremony,
    kind: z.literal('superseding_ceremony_required')
});

export const zCommittedSupersedingPublicCommandReceiptResponse = z.strictObject({
    receipt: zCommittedDeliveryConsumedPublicCommandReceipt,
    reconciliation: zSupersedingCeremonyReconciliation
});

export const zPublicCommandReceiptResponse = z.union([
    zAcceptedPublicCommandReceiptResponse,
    zOutcomeUnknownPublicCommandReceiptResponse,
    zCommittedTerminalPublicCommandReceiptResponse,
    zRejectedTerminalPublicCommandReceiptResponse,
    zCommittedSupersedingPublicCommandReceiptResponse,
    zCommittedSupersededPublicCommandReceiptResponse
]);

/**
 * The complete safe selector snapshot for one enabled product surface. A music, image, or video option may resolve internally to both a generation model and a main chat/orchestration model; the browser never selects or learns those base bindings independently.
 */
export const zSurfaceModelOptionCatalog = z.strictObject({
    catalogRevisionRef: z.string().min(1).max(256),
    defaultModelOptionRevisionRef: z.string().min(1).max(256),
    options: z.array(zPublishedModelOption).min(1).max(256),
    publishedAt: z.iso.datetime(),
    surfaceId: z.string().min(1).max(128)
});

/**
 * Deployment-level Site authority. It is resolved exclusively from the authenticated ProductWorkload and registered deployment; it never contains user-specific state.
 */
export const zProductContext = z.strictObject({
    agentCatalogRef: z.string().min(1).max(256),
    audience: z.string().min(1).max(256),
    cacheMaxAgeSeconds: z.int().gte(0).lte(300),
    deploymentRef: z.string().min(1).max(256),
    enabledSurfaceIds: z.array(z.string().min(1).max(128)).max(256),
    expiresAt: z.iso.datetime(),
    featurePolicyRevision: z.string().min(1).max(128),
    issuedAt: z.iso.datetime(),
    localePolicy: zLocalePolicy,
    modelOptionCatalogRef: z.string().min(1).max(256),
    modelOptionCatalogs: z.array(zSurfaceModelOptionCatalog).max(64),
    policyEpoch: zPositiveUint64String,
    productContextRef: z.string().min(1).max(256),
    region: z.string().min(1).max(128),
    revocationEpoch: zPositiveUint64String,
    runtimeEnvironment: z.enum([
        'development',
        'preview',
        'production'
    ]),
    sessionContractRevision: z.string().min(1).max(128),
    siteProjectBindingRef: z.string().min(1).max(256),
    siteRef: z.string().min(1).max(128),
    siteReleaseRef: z.string().min(1).max(128),
    webArtifactDigest: z.string().regex(/^[0-9a-f]{64}$/)
});

export const zProductContextExchangeResponse = z.strictObject({
    context: zProductContext,
    receipt: zCommandReceipt
});

export const zTotpConfirmationInput = z.strictObject({
    code: z.string().min(6).max(32),
    transactionRef: z.string().min(1).max(128)
});

export const zTotpEnrollmentStartInput = z.union([
    zBeginTotpEnrollmentInput,
    zSupersedeTotpEnrollmentInput
]);

export const zTotpEnrollmentTransaction = z.strictObject({
    expiresAt: z.iso.datetime(),
    manualEntrySecret: z.string().min(16).max(256),
    otpauthUri: z.string().min(1).max(2048),
    transactionRef: z.string().min(1).max(128)
});

/**
 * Available only on the fresh enrollment command's first successful response claim.
 */
export const zOneTimeTotpEnrollmentDelivery = z.strictObject({
    commandId: z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    requestDigest: z.string().regex(/^[0-9a-f]{64}$/),
    transaction: zTotpEnrollmentTransaction
});

/**
 * First-claim replacement ceremony delivery. Recovery does not commit and old factors remain authoritative until this pending seed is challenged successfully.
 */
export const zOneTimeTotpRecoveryEnrollmentDelivery = z.strictObject({
    commandId: z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    factorReplacementCapability: z.string().min(32).max(2048),
    requestDigest: z.string().regex(/^[0-9a-f]{64}$/),
    transaction: zTotpEnrollmentTransaction
});

export const zAccountRecoveryCompletionResponse = z.union([
    zCommandReceiptResponse,
    zOneTimeTotpRecoveryEnrollmentDelivery,
    zOneTimeDeliveryUnavailable
]);

export const zTotpEnrollmentTransactionResponse = z.union([
    zOneTimeTotpEnrollmentDelivery,
    zOneTimeDeliveryUnavailable
]);

export const zTransactionSecretInput = z.strictObject({
    transactionSecret: z.string().min(32).max(2048)
});

export const zUsageCreditAllocation = z.strictObject({
    amount: zCreditAmount,
    creditGrantId: z.string().regex(/^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/),
    journalReceiptRef: z.string().min(1).max(256)
});

export const zUsageDetail = z.strictObject({
    allocations: z.array(zUsageCreditAllocation).max(256),
    estimatedAmount: zCreditAmount,
    executionBudgetRootRef: z.string().min(1).max(256),
    occurredAt: z.iso.datetime(),
    ratedAmount: zCreditAmount.nullable(),
    runRef: z.string().min(1).max(256),
    settledAt: z.iso.datetime().nullable(),
    state: z.enum([
        'reserved',
        'rated',
        'settled',
        'reversed',
        'reconciliation_required'
    ]),
    unit: z.string().min(1).max(64),
    usageId: z.string().regex(/^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/)
});

export const zUsageDetailResponse = z.strictObject({
    freshness: zProjectionFreshness,
    usage: zUsageDetail
});

/**
 * Durable proof that verification, Site-local account activation, and the complete personal Workspace bootstrap committed atomically. Email verification never creates a browser session.
 */
export const zVerificationActivationResponse = z.strictObject({
    accountRef: z.string().min(1).max(128),
    receipt: zCommandReceipt
});

export const zCommandId = z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/);

/**
 * Stable caller-generated 128-bit command identity, encoded as lowercase 32-hex or UUIDv7. It must not be derived from Idempotency-Key, password, OTP, or request content. Platform owns a unique workload/Site/caller/command row and rejects the same command id with a different payload using an internal keyed digest; all responses and receipts echo this exact id.
 */
export const zCommandIdentity = z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/);

export const zContractVersion = z.literal('1');

export const zCreditGrantId = z.string().regex(/^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/);

export const zCsrfToken = z.string().min(32).max(512);

export const zIdempotencyKey = z.string().min(16).max(191);

export const zRedemptionId = z.string().regex(/^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/);

export const zTransactionRef = z.string().min(1).max(128);

export const zUsageId = z.string().regex(/^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/);

export const zCommandRequest = zCommandInput;

export const zCreateSessionRequest = zCreateSessionInput;

export const zEmailChangeCompletionRequest = zEmailChangeCompletionInput;

export const zEmailChangeStartRequest = zEmailChangeStartInput;

export const zIdentifierRequest = zIdentifierInput;

export const zOtpRequest = zOtpInput;

export const zPasswordChangeRequest = zPasswordChangeInput;

export const zPasswordRequest = zPasswordInput;

export const zReauthenticationRequest = zReauthenticationInput;

export const zRecoveryCodeRegenerationRequest = zRecoveryCodeRegenerationInput;

export const zRecoveryRequest = zRecoveryInput;

export const zRedemptionConfirmRequest = zRedemptionConfirmInput;

export const zRedemptionPreviewRequest = zRedemptionPreviewInput;

export const zRefreshSessionRequest = zRefreshSessionInput;

export const zRegistrationRequest = zRegistrationInput;

export const zRevokeRequest = zRevokeInput;

export const zSessionAccessGrantRequest = zSessionAccessGrantInput;

export const zSessionMfaCompletionRequest = zSessionMfaCompletionInput;

export const zTotpConfirmationRequest = zTotpConfirmationInput;

export const zTotpEnrollmentStartRequest = zTotpEnrollmentStartInput;

export const zTransactionSecretRequest = zTransactionSecretInput;

export const zGetPublicCommandReceiptHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1')
});

export const zGetPublicCommandReceiptPath = z.strictObject({
    id: z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/)
});

/**
 * A caller-and-digest-bound command receipt without replayable one-time payloads.
 */
export const zGetPublicCommandReceiptResponse = zPublicCommandReceiptResponse;

export const zBeginAccountRecoveryBody = zIdentifierRequest;

export const zBeginAccountRecoveryHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1'),
    'X-Kokoro-Command-Id': z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    'Idempotency-Key': z.string().min(16).max(191),
    'X-CSRF-Token': z.string().min(32).max(512)
});

/**
 * A non-disclosing account-recovery transaction.
 */
export const zBeginAccountRecoveryResponse = zAccountRecoveryTransactionResponse;

export const zCompleteAccountRecoveryBody = zRecoveryRequest;

export const zCompleteAccountRecoveryHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1'),
    'X-Kokoro-Command-Id': z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    'Idempotency-Key': z.string().min(16).max(191),
    'X-CSRF-Token': z.string().min(32).max(512)
});

export const zCompleteAccountRecoveryPath = z.strictObject({
    id: z.string().min(1).max(128)
});

/**
 * Either a durable password/TOTP-confirmation receipt, a first-claim TOTP replacement seed, or a typed instruction to reconcile and supersede a delivery that can no longer be replayed.
 */
export const zCompleteAccountRecoveryResponse = zAccountRecoveryCompletionResponse;

export const zBeginEmailChangeBody = zEmailChangeStartRequest;

export const zBeginEmailChangeHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1'),
    'X-Kokoro-Command-Id': z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    'Idempotency-Key': z.string().min(16).max(191),
    'X-CSRF-Token': z.string().min(32).max(512)
});

/**
 * An email-change verification transaction.
 */
export const zBeginEmailChangeResponse = zEmailChangeTransactionResponse;

export const zCompleteEmailChangeBody = zEmailChangeCompletionRequest;

export const zCompleteEmailChangeHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1'),
    'X-Kokoro-Command-Id': z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    'Idempotency-Key': z.string().min(16).max(191),
    'X-CSRF-Token': z.string().min(32).max(512)
});

export const zCompleteEmailChangePath = z.strictObject({
    id: z.string().min(1).max(128)
});

/**
 * A rotated opaque session and refresh credential pair.
 */
export const zCompleteEmailChangeResponse = zSessionCredentialPairResponse;

export const zBeginPasswordResetBody = zIdentifierRequest;

export const zBeginPasswordResetHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1'),
    'X-Kokoro-Command-Id': z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    'Idempotency-Key': z.string().min(16).max(191),
    'X-CSRF-Token': z.string().min(32).max(512)
});

/**
 * A non-disclosing password-reset transaction.
 */
export const zBeginPasswordResetResponse = zPasswordResetTransactionResponse;

export const zCompletePasswordResetBody = zPasswordRequest;

export const zCompletePasswordResetHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1'),
    'X-Kokoro-Command-Id': z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    'Idempotency-Key': z.string().min(16).max(191),
    'X-CSRF-Token': z.string().min(32).max(512)
});

export const zCompletePasswordResetPath = z.strictObject({
    id: z.string().min(1).max(128)
});

/**
 * An authoritative idempotent command receipt with no replayable secret payload.
 */
export const zCompletePasswordResetResponse = zCommandReceiptResponse;

export const zChangePasswordBody = zPasswordChangeRequest;

export const zChangePasswordHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1'),
    'X-Kokoro-Command-Id': z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    'Idempotency-Key': z.string().min(16).max(191),
    'X-CSRF-Token': z.string().min(32).max(512)
});

/**
 * A rotated opaque session and refresh credential pair.
 */
export const zChangePasswordResponse = zSessionCredentialPairResponse;

export const zRegenerateRecoveryCodesBody = zRecoveryCodeRegenerationRequest;

export const zRegenerateRecoveryCodesHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1'),
    'X-Kokoro-Command-Id': z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    'Idempotency-Key': z.string().min(16).max(191),
    'X-CSRF-Token': z.string().min(32).max(512)
});

/**
 * A one-time recovery set. Only hashes are retained by Platform.
 */
export const zRegenerateRecoveryCodesResponse = zRecoveryCodeSetResponse;

export const zBeginRegistrationBody = zRegistrationRequest;

export const zBeginRegistrationHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1'),
    'X-Kokoro-Command-Id': z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    'Idempotency-Key': z.string().min(16).max(191),
    'X-CSRF-Token': z.string().min(32).max(512)
});

/**
 * An email-verification transaction; delivery details are never disclosed.
 */
export const zBeginRegistrationResponse = zEmailVerificationTransactionResponse;

export const zListIdentitySessionsHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1')
});

/**
 * Caller-scoped device and identity sessions.
 */
export const zListIdentitySessionsResponse = zIdentitySessionList;

export const zCreateIdentitySessionBody = zCreateSessionRequest;

export const zCreateIdentitySessionHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1'),
    'X-Kokoro-Command-Id': z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    'Idempotency-Key': z.string().min(16).max(191),
    'X-CSRF-Token': z.string().min(32).max(512)
});

/**
 * Either an MFA challenge or a newly issued opaque credential pair.
 */
export const zCreateIdentitySessionResponse = zCreateSessionResponse;

export const zCompleteSessionMfaBody = zSessionMfaCompletionRequest;

export const zCompleteSessionMfaHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1'),
    'X-Kokoro-Command-Id': z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    'Idempotency-Key': z.string().min(16).max(191),
    'X-CSRF-Token': z.string().min(32).max(512)
});

export const zCompleteSessionMfaPath = z.strictObject({
    id: z.string().min(1).max(128)
});

/**
 * A rotated opaque session and refresh credential pair.
 */
export const zCompleteSessionMfaResponse = zSessionCredentialPairResponse;

export const zReauthenticateIdentitySessionBody = zReauthenticationRequest;

export const zReauthenticateIdentitySessionHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1'),
    'X-Kokoro-Command-Id': z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    'Idempotency-Key': z.string().min(16).max(191),
    'X-CSRF-Token': z.string().min(32).max(512)
});

/**
 * A short-lived proof for a sensitive operation.
 */
export const zReauthenticateIdentitySessionResponse = zReauthenticationResponse;

export const zRefreshIdentitySessionBody = zRefreshSessionRequest;

export const zRefreshIdentitySessionHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1'),
    'X-Kokoro-Command-Id': z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    'Idempotency-Key': z.string().min(16).max(191),
    'X-CSRF-Token': z.string().min(32).max(512)
});

/**
 * A rotated opaque session and refresh credential pair.
 */
export const zRefreshIdentitySessionResponse = zSessionCredentialPairResponse;

export const zRevokeIdentitySessionsBody = zRevokeRequest;

export const zRevokeIdentitySessionsHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1'),
    'X-Kokoro-Command-Id': z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    'Idempotency-Key': z.string().min(16).max(191),
    'X-CSRF-Token': z.string().min(32).max(512)
});

/**
 * An authoritative idempotent command receipt with no replayable secret payload.
 */
export const zRevokeIdentitySessionsResponse = zCommandReceiptResponse;

export const zConfirmTotpEnrollmentBody = zTotpConfirmationRequest;

export const zConfirmTotpEnrollmentHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1'),
    'X-Kokoro-Command-Id': z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    'Idempotency-Key': z.string().min(16).max(191),
    'X-CSRF-Token': z.string().min(32).max(512)
});

/**
 * A one-time recovery set. Only hashes are retained by Platform.
 */
export const zConfirmTotpEnrollmentResponse = zRecoveryCodeSetResponse;

export const zDisableTotpBody = zOtpRequest;

export const zDisableTotpHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1'),
    'X-Kokoro-Command-Id': z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    'Idempotency-Key': z.string().min(16).max(191),
    'X-CSRF-Token': z.string().min(32).max(512)
});

/**
 * An authoritative idempotent command receipt with no replayable secret payload.
 */
export const zDisableTotpResponse = zCommandReceiptResponse;

export const zBeginTotpEnrollmentBody = zTotpEnrollmentStartRequest;

export const zBeginTotpEnrollmentHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1'),
    'X-Kokoro-Command-Id': z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    'Idempotency-Key': z.string().min(16).max(191),
    'X-CSRF-Token': z.string().min(32).max(512)
});

/**
 * A one-time TOTP enrollment secret. It is never recoverable from a receipt.
 */
export const zBeginTotpEnrollmentResponse = zTotpEnrollmentTransactionResponse;

export const zCompleteEmailVerificationBody = zTransactionSecretRequest;

export const zCompleteEmailVerificationHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1'),
    'X-Kokoro-Command-Id': z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    'Idempotency-Key': z.string().min(16).max(191),
    'X-CSRF-Token': z.string().min(32).max(512)
});

export const zCompleteEmailVerificationPath = z.strictObject({
    id: z.string().min(1).max(128)
});

/**
 * The verified account activation result.
 */
export const zCompleteEmailVerificationResponse = zVerificationActivationResponse;

export const zResendEmailVerificationBody = zIdentifierRequest;

export const zResendEmailVerificationHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1'),
    'X-Kokoro-Command-Id': z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    'Idempotency-Key': z.string().min(16).max(191),
    'X-CSRF-Token': z.string().min(32).max(512)
});

/**
 * An email-verification transaction; delivery details are never disclosed.
 */
export const zResendEmailVerificationResponse = zEmailVerificationTransactionResponse;

export const zGetCreditGrantHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1')
});

export const zGetCreditGrantPath = z.strictObject({
    id: z.string().regex(/^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/)
});

/**
 * Safe source and balance details for one authenticated account CreditGrant.
 */
export const zGetCreditGrantResponse = zCreditGrantResponse;

export const zGetCreditSummaryHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1')
});

/**
 * Rebuildable credit projection over immutable grants, journal entries, and holds.
 */
export const zGetCreditSummaryResponse = zCreditSummaryResponse;

export const zGetPersonalContextHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1')
});

/**
 * The caller's Site-bound personal workspace, project, and execution space.
 */
export const zGetPersonalContextResponse = zPersonalContext;

export const zListAccountProductsHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1')
});

/**
 * Effective products, subscriptions, and entitlements for the authenticated account.
 */
export const zListAccountProductsResponse = zAccountProductsResponse;

export const zGetUsageDetailHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1')
});

export const zGetUsageDetailPath = z.strictObject({
    id: z.string().regex(/^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/)
});

/**
 * Rated usage and allocation lineage without provider credentials or GA internals.
 */
export const zGetUsageDetailResponse = zUsageDetailResponse;

export const zExchangeProductContextBody = zCommandRequest;

export const zExchangeProductContextHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1'),
    'X-Kokoro-Command-Id': z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    'Idempotency-Key': z.string().min(16).max(191),
    'X-CSRF-Token': z.string().min(32).max(512)
});

/**
 * A Site-release-bound product context.
 */
export const zExchangeProductContextResponse = zProductContextExchangeResponse;

export const zRecoverRedemptionCommandHeaders = z.strictObject({
    'Idempotency-Key': z.string().min(16).max(191),
    'Kokoro-Contract-Version': z.literal('1')
});

/**
 * The current durable state of one authenticated redemption command.
 */
export const zRecoverRedemptionCommandResponse = zRedemptionCommandResponse;

export const zGetRedemptionReceiptHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1')
});

export const zGetRedemptionReceiptPath = z.strictObject({
    id: z.string().regex(/^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/)
});

/**
 * An immutable safe redemption and Fulfillment receipt.
 */
export const zGetRedemptionReceiptResponse = zRedemptionReceiptResponse;

export const zConfirmRedemptionBody = zRedemptionConfirmRequest;

export const zConfirmRedemptionHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1'),
    'X-Kokoro-Command-Id': z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    'Idempotency-Key': z.string().min(16).max(191),
    'X-CSRF-Token': z.string().min(32).max(512)
});

/**
 * The current durable state of one authenticated redemption command.
 */
export const zConfirmRedemptionResponse = zRedemptionCommandResponse;

export const zPreviewRedemptionBody = zRedemptionPreviewRequest;

export const zPreviewRedemptionHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1'),
    'X-Kokoro-Command-Id': z.string().regex(/^(?:[0-9a-f]{32}|[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$/),
    'Idempotency-Key': z.string().min(16).max(191),
    'X-CSRF-Token': z.string().min(32).max(512)
});

/**
 * Safe, non-binding redemption terms and a short-lived opaque preview credential.
 */
export const zPreviewRedemptionResponse = zRedemptionPreviewResponse;

export const zIssueSessionAccessGrantBody = zSessionAccessGrantRequest;

export const zIssueSessionAccessGrantHeaders = z.strictObject({
    'Kokoro-Contract-Version': z.literal('1'),
    'X-CSRF-Token': z.string().min(32).max(512)
});

/**
 * A non-cacheable, short-lived Session credential bound to the exact product and subject context.
 */
export const zIssueSessionAccessGrantResponse = zSessionAccessGrantResponse;
