{
  "name": "@kokoro/tsconfig",
  "version": "0.1.0",
  "private": true,
  "description": "Kokoro web monorepo 共享 TypeScript 基线配置。apps/user、apps/admin 各自 extends base.json，只保留 app 专属(paths/plugins/types/include)。",
  "files": [
    "base.json"
  ]
}
