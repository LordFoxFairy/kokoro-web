# Admin URL 状态测试报告

## 结论

| 字段 | 结果 |
|---|---|
| 批次 | App Router list and diagnostic search state |
| 代码基线 | `c772086` + 本批次未提交 URL 状态变更 |
| 执行时间 | 2026-08-18T13:25:08-04:00 至 2026-08-18T13:25:41-04:00 |
| 分类状态 | `PASS` |
| Admin 整体状态 | `NOT_READY` |
| Skip / Todo / Retry | `0 / 0 / 0` |

## 自动化结果

| 门禁 | 命令 | 结果 |
|---|---|---|
| 单元测试 | `pnpm test` | PASS；5 files，41 tests |
| TypeScript | `pnpm typecheck` | PASS |
| ESLint | `pnpm lint` | PASS；0 warnings |
| Prettier | `pnpm format:check` | PASS |
| Production build | `pnpm build` | PASS；Next.js 16.2.6 |
| Diff hygiene | `git diff --check` | PASS |

## 本批次覆盖

- 用户、Organization 和 Site：查询、状态、排序、方向、删除项和 Organization 过滤。
- 角色：scope、角色选择、删除项和查询。
- 权限诊断：主体、scope、资源和操作；不计算授权结果。
- 会话：查询、用户和多状态筛选。
- 审计：查询、操作者、目标、request ID、结果和 scope。
- Next search params 数组输入、非法枚举、空值、未知字段和稳定 round-trip。
- 所有序列化入口统一返回 `URLSearchParams`。
- 不透明 `pageToken` 不进入 URL；解析时忽略该字段。

## 未验收项

- 页面组件、浏览器前进后退和刷新后的真实渲染。
- 官方 shadcn/ui Sidebar、Table、Dialog、Sheet 和表单。
- Auth.js、权限路由保护、真实 RPC 和 IAM 授权。
- 响应式、可访问性、性能、FOUC 和两轮浏览器 E2E。

以上项目继续保持 `NOT_READY`。
