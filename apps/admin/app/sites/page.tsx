import { ResourceTable } from "@/components/shell/resource-table";

export default function Page(): React.ReactElement {
  return <ResourceTable moduleId="site" title="站点" subtitle="站点、域名、应用、策略与功能开关。" />;
}
