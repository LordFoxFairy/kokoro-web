name: CI

on:
  push:
  pull_request:

jobs:
  gates:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: install pinned pnpm
        uses: pnpm/action-setup@8912a9102ac27614460f54aedde9e1e7f9aec20d # v6.0.5
        with:
          version: 11.2.2
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - name: production dependency audit
        run: pnpm audit --prod --audit-level high
      - name: typecheck
        run: pnpm -r typecheck
      - name: lint
        run: pnpm -r lint
      - name: test
        run: pnpm test
      - name: build Site factory and reference fixture
        run: pnpm run build:site
      - name: verify tracked Site BFF import artifact
        run: pnpm --filter @kokoro/site-bff verify:dist
      - name: build admin console
        run: pnpm run build:admin
      - name: build dedicated Admin production image
        run: docker build --file apps/admin/Dockerfile --tag kokoro/admin-web:${{ github.sha }} .
