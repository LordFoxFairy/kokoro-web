# dependencies
node_modules/

# next build output
.next/
out/

# production
build/
coverage/
/apps/user/docs/*
!/apps/user/docs/README.md
!/apps/user/docs/web/
!/apps/user/docs/web/**

# local env files
.env*
!.env.example

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# system / editor
.DS_Store
.idea/
.vscode/

# deployment
.vercel/

# typescript
*.tsbuildinfo
next-env.d.ts

# test artifacts
playwright-report/
vitest-report/

# local agent helper
CLAUDE.md

# local agent / MCP tooling scratch
.claude/
.jbeval/
.playwright-mcp/
.superpowers/

# 包管理器：本仓已转 pnpm workspace（apps/* + packages/*，与 kokoro-platform 一致）。
# pnpm-lock.yaml / pnpm-workspace.yaml 为正式锁与工作区声明，必须入库。npm package-lock 已废弃。

# e2e 实证截图(会话产物,不入库)
e2e-*.png
hitl-*.png
a-*.png
b-*.png
c-*.png
d-*.png
stream-*.png
team-owner-dropdown.png
teams-page.png

# 用户暂存区（kokoro-platform 等异构草稿）：不入库、不进 tsc/vitest（见 vitest.config / tsconfig exclude）。
/tmp

# gitwarp 本地 worktree/ledger 工具目录：不入库
.gitwarp/
